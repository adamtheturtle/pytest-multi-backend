Installation
------------

.. code-block:: console

   $ pip install pytest-multi-backend

This requires Python |minimum-python-version|\+.

Installing the package registers the ``pytest`` plugin, which adds the ``--skip-backend`` and ``--skip-marker`` options.
